import {escapeHtml as e} from './formatting.js';
import {formatMoneyInput} from './editor-state.js';
const input=(key,label,value,type='text')=>`<label class="life-field" for="life-${key}">${e(label)}<input id="life-${key}" data-life-input="${key}" type="${type}" ${type==='text'?'inputmode="decimal"':''} value="${e(value??'')}" placeholder="Not entered"></label>`;
export function lifePlanSetupMarkup(state){
 const {draft:m,readiness,validation}=state.editor,meta=m.extensions.production.template,issues=[...(validation.errors??[]),...(readiness.missing??[])];
 const select=(key,label,options,value)=>`<label class="life-field" for="life-${key}">${label}<select id="life-${key}" data-life-input="${key}">${options.map(([v,l])=>`<option value="${v}" ${v===value?'selected':''}>${l}</option>`).join('')}</select></label>`;
 return `<section class="view"><div class="page-heading"><div><p class="eyebrow">Your starting plan · unsaved</p><h1>Make Life Plan yours</h1><p>Current high-school junior → UT Knoxville → law school → Air Force JAG → optional household → long-term home → age 95.</p></div></div><section class="panel panel-pad"><h2>A few personal inputs</h2><p id="life-input-error" role="status">${e(Object.values(state.quickInputErrors??{}).join(" "))}</p><p>The dates and careers are already linked. Unknown balances stay unknown until you enter an amount or explicitly choose $0.</p><div class="life-fields">${input('birthYear','Birth year',m.people[0].birthYear,'number')}${select('filingStatus','Starting tax filing status',[['','Choose status'],['single','Single'],['marriedFilingJointly','Married filing jointly']],m.household.filingStatus??'')}${select('partner','Optional pediatrics partner path',[['no','Do not include yet'],['yes','Include — same-age planning assumption']],m.people[1].enabled?'yes':'no')}${input('bah','Monthly BAH planning estimate ($, 2026 dollars)',formatMoneyInput(m.careers[0].compensationRule.bahFallback?.monthlyCents))}</div><p class="muted">BAH is your editable estimate, not a duty-station quote. Partner timing can be changed in detailed setup; combining finances at 29 does not change tax filing status.</p><h2>Starting account balances</h2><div class="life-fields">${m.accounts.map(a=>input('balance:'+a.id,a.name+' ($)',formatMoneyInput(a.openingBalanceCents))).join('')}</div><button class="button small" data-action="zero-balances">Set remaining unknown balances to $0</button><p>Education and other debts: none are invented. Add any balances, rates and payments in <button class="text-button" data-action="detailed-setup">Detailed Model Setup</button>. School funding and future debt remain optional to customize, but matter to the results.</p></section><section class="panel panel-pad"><h2>Review the planning assumptions</h2><p>${e(meta.spendingNote)}</p><p>Six months of essential expenses in household reserves. Planned JAG retirement contributions: 10%; taxable investing: $6,000/year when feasible. Retirement accounts are protected from optional purchases.</p><p>$1.5 million long-term home in 2025 dollars, Texas / North Dallas; no purchase age assumed. Expected $200,000 paid-off property transfer has no date and adds nothing to wealth until scheduled and realized.</p><p>${e(meta.homeCostNote)}</p><p class="status-tag warning">${e(meta.retirementNote)}</p><p>Future promotions are editable planning events. No prior military service assumed. Spending before career income may produce deficits: enter actual school funding, work or family support in detailed setup.</p><details><summary>Readiness details</summary><div data-life-readiness>${issues.length?`<ul>${issues.map(i=>`<li>${e(i.message)}</li>`).join('')}</ul>`:'<p>All required inputs are complete. Projection-ready does not mean financially feasible.</p>'}</div></details><div class="button-row"><button class="button primary" data-editor-action="save" ${readiness.ready&&!Object.keys(state.quickInputErrors??{}).length?'':'disabled'}>Save &amp; explore Life Plan</button><button class="button" data-action="detailed-setup">Detailed Model Setup</button></div></section></section>`;
}
